# Flask Web Framework

flask==3.0.0
jinja2==3.1.3           # Template Engine (Flask sử dụng Jinja2 mặc định)
flask-cors               # Xử lý CORS cho API bảo mật khi gọi từ frontend khác domain

#  Security & Environment

python-dotenv            # Đọc file .env, quản lý biến môi trường an toàn
flask-limiter            # (Optional) Giới hạn tần suất request API, chống spam

#  Machine Learning & Deep Learning

tensorflow==2.13.0
keras==2.13.1
scikit-learn
statsmodels
joblib

# Data Processing

pandas
numpy
scipy
yfinance                 # Lấy dữ liệu chứng khoán

#  Visualization Libraries

matplotlib
seaborn
plotly

#  API & HTTP Requests

requests                 # Gửi HTTP requests (GET, POST,...)
httpx                    # (Optional) Hỗ trợ HTTP/2, async requests

#  Utilities (Optional)

tqdm                     # Hiển thị progress bar cho các vòng lặp
gunicorn                 # WSGI server, dùng khi deploy production

